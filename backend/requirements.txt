# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.0.0
python-dotenv>=1.0.0

# File Processing
pymupdf>=1.23.0
python-docx>=1.1.0
pytesseract>=0.3.10
easyocr>=1.7.0          # fallback OCR engine (no Tesseract binary needed)
Pillow>=10.0.0
langdetect>=1.0.9

# Embeddings
sentence-transformers>=2.7.0
torch>=2.0.0

# LangChain Orchestration
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-mistralai>=0.1.0
langchain-text-splitters>=0.2.0

# FAISS Vector Store (replaces ChromaDB)
faiss-cpu>=1.7.4

# Mistral LLM (Primary)
mistralai>=1.0.0

# OpenAI (Legacy fallback, optional)
openai>=1.30.0

# Ollama (Local LLM — CR-01 primary offline provider)
# No SDK required: talked to directly over HTTP via httpx (see llm_provider_ollama.py)

# Database + Auth (CR-01: users, documents, conversations, chat_messages, models)
sqlalchemy>=2.0.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1            # pinned: passlib 1.7.4's version probe breaks on bcrypt>=4.1 (raises on hash())
PyJWT>=2.8.0
email-validator>=2.1.0   # required by pydantic EmailStr

# Utilities
aiofiles>=23.2.1
httpx>=0.27.0
numpy>=1.26.0

# Audio/Video transcription (optional)
# openai-whisper>=20231117
